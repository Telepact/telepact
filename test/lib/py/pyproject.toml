[project]
name = "telepacttest"
version = "0.1.0"
requires-python = ">=3.11,<4"
dependencies = [
    "build>=1.6.0,<2",
    "msgpack>=1.2.2,<2",
    "nats-py>=2.15.0,<3",
    "prometheus-client>=0.26.0,<0.27",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1,<10",
    "toml>=0.10.2,<0.11",
]

[tool.uv]
package = false
