# ts-http-cookie-auth

Minimal TypeScript Telepact example that shows how to pull a session cookie into `@auth_` at the HTTP boundary and resolve it with `ServerOptions.onAuth`.

Run it:

```bash
make run
```
