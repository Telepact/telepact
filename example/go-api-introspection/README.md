# go-api-introspection

Minimal Go Telepact server showing the built-in `fn.api_` schema endpoint.

Commands:

- `make run` — start the server on `http://127.0.0.1:8097`
- `make check` — verify `fn.ping` and confirm `fn.api_` exposes the schema
