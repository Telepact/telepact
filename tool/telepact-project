#!/usr/bin/env bash

#|
#|  Copyright The Telepact Authors
#|  SPDX-License-Identifier: Apache-2.0
#|

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

exec uv run --project "${SCRIPT_DIR}/telepact_project_cli" python -m telepact_project_cli.cli "$@"
