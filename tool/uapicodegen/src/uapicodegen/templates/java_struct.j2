public class {{ name }} {
    {% for field in fields %}
    private {{ field.data_type }} {{ field.name }};
    {% endfor %}

    public {{ name }}({% for field in fields %}{{ field.data_type }} {{ field.name }}{% if not loop.last %}, {% endif %}{% endfor %}) {
        {% for field in fields %}
        this.{{ field.name }} = {{ field.name }};
        {% endfor %}
    }
}