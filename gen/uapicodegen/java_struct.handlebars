/**
 {{#each docstring}}
   * {{line}}
 {{/each}}
 */
public class {{name}} {
    {{#each fields}}
        public final {{type}} {{name}};
    {{/each}}

    public {{name}}(
        {{#each fields}}
            {{type}} {{name}}{{#comma}},{{/comma}}
        {{/each}}
    ) {
        {{#each fields}}
            this.{{name}} = {{name}};
        {{/each}}
    }

    public {{name}}(
        Map<String, Object> pseudoJson
    ) {
        {{#each fields}}
            this.{{name}} = pseudoJson.get("{{name}}{{#if optional}}!{{#if}}");
        {{/each}}
    }
}